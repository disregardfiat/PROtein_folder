# PROtein — base (Python 3.10+)
numpy>=1.22
scipy>=1.9

# Optional but recommended: pyhqiv — metric math (φ, γ, Θ) and tree-torque discrete φ/ψ (pyhqiv.molecular)
# Required for extrusion + 3-cycle pipeline (run_discrete_refinement). Install with: pip install pyhqiv
pyhqiv
